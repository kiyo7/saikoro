import React from 'react';
import styled from 'styled-components';

export const Title = () => {
  return (
    <>
      <STitle>tintiro</STitle>
    </>
  );
};

const STitle = styled.h1`
  font-size: 40px;
  text-align: center;
  color: black;
`;
